<h1><%= @question.title %></h1>

<p><%= @question.body %></p>


<h5>Answered?
<%= check_box( "question", "resolved") %></h5>
<%= link_to "Edit/Delete", edit_question_path(@question), class: 'btn btn-success' %>
